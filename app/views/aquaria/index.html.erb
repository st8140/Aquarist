<%= provide(:title, "aquaria") %>
<%= render "aquaria/new" %>

<div class="container py-4">
  <div class="row">
    <%= render "navbar" %>
    <div class="p-4 bg-color">
      <div class="row">
        <%= render partial: "posts", collection: @aquaria, as: "aquarium" %>
      </div>
    </div>
  </div>
</div>